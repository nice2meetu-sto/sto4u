# 서울관광재단 인력현황 관리 시스템 (sto4u) — 인수인계 / 개발 가이드

> 이 문서는 클라우드 세션에서 개발된 전체 이력·규칙의 압축본입니다.
> **개발 전 반드시 이 문서를 따르고, 완료 후 "자체검증" 섹션대로 확인하세요.**

## 1. 프로젝트 소개

2023-12-30 기준 명부(스냅샷) + 인사발령 리플레이로 **임의 기준일의 인력현황을 재구성**하는 단일 페이지 웹앱.
Google Sheets가 DB, Apps Script(GET JSON API)가 백엔드, GitHub Pages로 호스팅.

- 헤더: `서울관광재단` 라벨 + `인력현황표`(19px/900) + `인원현황 대시보드` 링크(`hrtoday.html` 상대경로, 같은 호스팅 폴더의 별도 대시보드로 이동)
- 탭: ① 일일 ② 직급별 ③ 조직도 ④ 전체 재직자 ⑤ 인사발령 등록 ⑥ 입퇴사자 등록

## 2. 절대 제약 (변경 금지)

1. **단일 HTML + 순수 JS** — 빌드/번들러/프레임워크 금지. GitHub Pages 호스팅 가능해야 함.
2. **Apps Script GET 전용 JSON API** — `fetch` GET만. POST 금지(CORS preflight 회피), `google.script.run`/`HtmlService` 금지.
3. **localStorage/sessionStorage 금지** — 상태는 메모리(state 객체)만.
4. 폰트 **Noto Sans KR** (400/500/700/900 로드됨 — 900은 헤더 타이틀용).
5. 색 토큰: `--main:#534AB7` `--valid:#3B6D11` `--warn:#A32D2D` `--border:#e2e4ea`.
6. **모든 날짜는 KST**, `yyyy-MM-dd` 문자열 비교.

## 3. 파일 구성

| 파일 | 역할 | 수정 여부 |
|---|---|---|
| `index.html` | **메인 앱 (유일한 수정 대상)** | ✅ 여기만 수정 |
| `Code.gs` | Apps Script (읽기 + addOrder/addBase/setLeave) | 수정 시 **재배포 필요** |
| `일일현황표.html` `직급별현황표.html` `정현원비교표.html` | 타 시스템에 이식용 독립 파일 | ❌ 이미 공유 완료 — **수정 금지** |
| `header-snippet.html` | 타 페이지용 헤더 이식 스니펫 | 참고용 |
| `README.md` | 배포/데이터 모델 문서 | 참고용 |

- `index.html` 상단 상수: `SCRIPT_URL`(Apps Script 배포 URL — 사용자가 직접 교체하기도 함), `SHEET_URL`+`SHEET_PASSWORD`("stohr", DB 버튼용), `BASE_DATE`("2023-12-30"), `BONBU_ORDER`, `DEPT_ORDER`, `CENTER_ORDER`.
- **Code.gs 수정 시**: Apps Script 편집기에 붙여넣고 [배포]→[배포 관리]→기존 배포에 **새 버전**으로 배포하면 URL 유지됨. 새 배포를 만들면 URL이 바뀌므로 `SCRIPT_URL` 교체 필요.

## 4. 데이터 모델 (Google Sheets 탭)

Apps Script `readSheet`는 **헤더 이름 기반**이라 열 순서 무관, 열 추가해도 안 깨짐.

- **기초명부**: 사원코드(=사번), 사원명, 부서코드, 직급, 직책, 고용형태, 성별, 근무지(=사업장), 입사일, 퇴사일, 대체, 비고, 상태
  - 역대 입/퇴사자 전원 보유. 입/퇴사는 **날짜로 자동 판정**(상태 라벨 아님).
- **인사발령**: 발령일, 사원코드, 사원명, 변동구분, 변경부서코드(변경부서), 변경직급, 변경직책(직책변경), 변경근무지, 변경고용형태, 비고
- **부서마스터**: 부서명, 본부, 부서코드, 표시순서, 적용일 — **유효일자(effective-dated)**. 같은 코드에 적용일별 여러 행.
- **정원**: 직급, 정원(명), 변경일 — 변경일 ≤ 기준일 중 최신값 적용.
- **기타_사원코드**: 사원명→코드 보조 맵.

### 핵심 날짜 규칙
- **재직 판정**: `입사일 ≤ 조회일 AND (퇴사일 없음 OR 조회일 ≤ 퇴사일)`
- **퇴사일 = 마지막 근무일** → 퇴사일 **당일까지 재직**, 다음날부터 제외. (퇴사 발령도 동일: 발령일 당일 재직)
- 입사예정(미래 입사일)은 입사일이 지나면 자동 재직 집계.

### 변동구분(발령 타입) — 동작 분기는 5가지뿐
`입사`(신규 추가) / `퇴사`(제외) / `휴직`·`파견`(상태) / `복직`(재직). **그 외(전보·승급 등)는 라벨일 뿐**이고,
실제 변경은 행의 `변경*` 컬럼 값이 **타입과 무관하게 항상** 적용됨(복합 발령 지원).
- 근무지 이동도 **전보**로 통일(드롭다운에서 '근무지이동' 제거됨, 동의어 맵엔 유지되어 과거 데이터 호환).
- 동의어: 전보→부서이동, 승격→승급, 퇴직→퇴사, 복귀→복직 등 (`ORDER_TYPE_SYNONYMS`).

### 부서마스터 규칙 (중요)
- `resolveDept(code, asOf)`: 적용일 ≤ asOf 중 최신 행 → 그 시점의 부서명/본부/표시순서.
- `resolveDeptCodeByName(name, asOf)`: **그 날짜에 유효한 이름만 매칭** (과거에만 쓰인 옛 팀명은 매칭 안 됨 — 절대 되돌리지 말 것).
- 조직개편 입력법: **개편일을 적용일로 전체 부서를 새로 나열**(기존 행 수정·삭제 금지 — 과거 조회가 깨짐).
- 표시순서는 "본부 안에서의 순서"만 의미. 본부 이동은 `본부` 값도 같이 변경.
- 본부만 이동 시 **부서코드 유지**하면 인원 자동 따라감. 코드를 바꾸면 인사발령으로 인원 이동 필요. 폐지팀 인원도 발령으로 이동 필요.

## 5. 집계/분류 규칙

- **일일표**: 고용형태(일반직/전문직/기간제/파견) × 남녀. 사고내역(휴직·휴가·파견·기타)은 **원소속 부서에 그대로 집계**(과거의 '인재경영팀(임) 버킷' 제도는 **제거됨**). 현재인원 = 소계 − 사고내역.
- **직급별표**: 기간제는 직급 무관 별도 칸. 전문직 고용형태는 직급 라벨 무관 '전문직' 칸.
- **정현원 비교표** 판정 순서(`jhCountHead`):
  1. **별도직군 = 비고에 `전문위원` 또는 `별도직군` 포함** (최우선, 직급·고용형태 무관, 직급 통계서 제외)
  2. 고용형태 기간제 → 기간제
  3. 고용형태 전문직 → 가~라급
  4. 나머지 → 임원~6급
  - 전문직 **정원**: 개별(가~라)값 있으면 그 합, 없으면 '전문직' 합계행 (중복 합산 금지). 다+라 정원 입력 여부로 통합 1칸 ↔ 가+나/다+라 2칸 표시 전환.
- **직책은 어떤 통계에도 영향 없음** (조직도/명단/DB 표시·정렬용).
- 기간제 직급엔 '주임/위원' 등 자유 사용 가능. 비기간제에 쓰면 '기타'로 빠지니 주의.
- 상태 동의어: 퇴사/입사예정 라벨은 '재직' 취급(날짜로 판정하므로), `STATUS_SYNONYMS`.

## 6. UI 동작 규칙

- **sticky-head**: 헤더+배너+조회기준일/타임라인이 한 덩어리로 상단 고정 (개별 sticky 금지).
- 기본 조회일 = **오늘(KST)** (BASE_DATE 이전이면 BASE_DATE). '오늘' 버튼 있음.
- **변동 표시**(토글, 기본 ON): 직전 변동 시점 대비 값이 바뀐 칸에 파란 테두리(`.chg`). 변동 시점 = 발령일 + 입사일 + **퇴사일+1** (`buildTimeline`). ◀변동/변동▶ 버튼과 슬라이더가 이 시점들을 순회.
- **빈 좌석 유지**: 대표이사·본부장(부서명=본부명) 행/카드는 인원 0이어도 유지(`seatDepts`).
- **센터별 근무자 현황**: 본사 제외, `CENTER_ORDER` 순서(미지정은 명부 등장순), 한 줄 최대 5칸 그리드.
- **조직도**(③): 탭 열 때만 렌더(lazy). 대표이사 단독 최상단 → 직속(대표이사실 소속: 감사관·홍보팀) → 3본부 컬럼(본부명 라벨 없음, 본부 박스만). 칩 정렬 `ORG_TITLE_ORDER`(대표이사>본부장>팀장>팀장대행>총괄매니저)→직급→입사일. 칩 색: 휴직 `hj`(회색), 파견 `pg`(회색+굵은 테두리), 기간제 `ct`(노랑). 대표이사·본부장 카드는 공석이어도 보라 테두리(lead). 대체근무는 팝업에 **이름만** 표시.
- **전체 재직자**(④): **행 전체 클릭** → 인사 카드+발령 이력 팝업(호버는 기존 회색 유지). 이력 표: `발령일·구분·변경부서·직급·직책·근무지`, 세로줄 없음, 구분 보라 강조, **오래된순**. 맨 위 합성 행: 입사일≤BASE_DATE→`2023-12-30·기초`, 이후→`입사일·입사` (기초명부 원본값). **부서명은 발령일 시점 기준 역추적**(`orderDeptName(o, 발령일)` — 조회일 기준으로 바꾸지 말 것).
- **CSV 다운로드**: 현재 필터된 재직자. **DB 다운로드**: 기초명부 **전원**(입사예정·퇴직자 포함)을 기준일 기준 가공, 컬럼 순서 `사원명 사번 부서 직급 직위 고용형태 성별 근무지 입사일 퇴사일 대체 비고` (타 시트 이식용).
- **인사발령 등록**(⑤): 이름→사번 자동, **동명이인은 흰 바탕 select**(사번·입사일 표시)로 선택. 부서명→발령일 기준 부서코드 자동 변환.
- **입퇴사자 등록**(⑥): 입사=여러 행 동시 입력(각자 전부 개별 입력, `addBase`, **상태=입사예정** 저장 — 안내문구엔 상태 언급 안 함), 대체(이름)→사번 자동. 퇴사=이름 검색(동명이인 select)→`setLeave`(**사번으로만 행 매칭**, 입사일·상태 안 봄).

## 7. 자체검증 (개발 후 필수)

`index.html`에 내장 자체검증 35개(`runSelfTests`)가 있음. Node로 실행:

```js
// t.js — <script> 추출 시 CSS 주석 속 "<script>" 문자열에 걸리지 않게 정규식/슬라이스 주의
const fs=require("fs");
let js=fs.readFileSync("index.html","utf8").match(/<script>([\s\S]*)<\/script>/)[1];
js=js.replace(/window\.addEventListener\("DOMContentLoaded"[\s\S]*$/, "");
global.document={getElementById:()=>({}),querySelectorAll:()=>[],createElement:()=>({addEventListener(){},querySelector:()=>({addEventListener(){}})})};
global.window={};
eval(js + `
state.base=DEMO.base; state.orders=DEMO.orders.map((o,i)=>({...o,__idx:i})); state.deptMaster=buildDeptMaster(DEMO.dept);
const r=runSelfTests(); if(r.passed!==r.total) throw new Error("FAIL "+r.passed+"/"+r.total);
console.log("PASS", r.total);`);
```

- 렌더 함수까지 테스트하려면 `getElementById`를 innerHTML 저장 stub으로 확장(과거 세션의 smoke 테스트 방식).
- 실데이터 검수 기준: 2023-12-30에 일일 합계 58/132/190, 휴직 15, 현재 175 / 직급별 소계 145·기간제 45·총계 190.
- 규칙 변경 시 관련 자체검증도 함께 갱신할 것(예: 퇴사일 경계 테스트).

## 8. Git / 협업 주의

- 작업 브랜치: `claude/upbeat-carson-lkpfp0` (로컬 전환 후에도 동일 브랜치 또는 main 병합은 사용자 결정).
- **사용자가 GitHub 웹에서 직접 커밋하는 경우가 있음**(주로 `SCRIPT_URL` 교체, 문구 수정) → **작업 전 반드시 pull**, 푸시 거부되면 `git pull --rebase` 후 푸시.
- 호스팅: GitHub Pages. 대시보드(`hrtoday.html`)와 같은 폴더에 배포됨. 게시본이 옛 파일이면 링크가 예전 것일 수 있으니 게시 위치 확인.

## 9. 개발 이력 하이라이트 (왜 이렇게 됐는지)

- 부서 관리는 "부서명 뒤 숫자" 방식 대신 **부서마스터 탭(유효일자)** 채택 — 조직개편 역사 정확성 때문.
- 사고내역 버킷(모드 A, 인재경영팀(임)) → **제거**. 휴직자는 실제로 인사팀(임) 등으로 발령 이동해 관리하므로 원소속 집계로 전환.
- 별도직군 판정: 고용형태 '개방형' 기준 → **비고 키워드('전문위원'/'별도직군') 단독 기준**으로 변경.
- 전문직 정원 소계: 개별행+합계행 중복 합산 버그 수정(개별 우선).
- `jhNum`은 parseFloat+round (parseInt("1.0")→10 버그 이력).
- `quotaAsOf`: 변경일이 비정상(시리얼 등)이면 BASE_DATE로 간주.
- 부서명→코드 변환은 "그 날짜에 유효한 이름"만 — 옛 팀명이 새 날짜에 매칭되던 버그 수정.
- 이력 표 부서명은 발령일 시점 역추적 — 조회일 기준이면 옛 발령이 현재 팀명으로 보이는 버그.
- setLeave는 사번만으로 매칭 — 입사일 대조는 시트 날짜 서식 차이로 실패했던 이력.
- 날짜 input은 `max="2100-12-31"` 필수(연도 4자리 자동 넘어감).
